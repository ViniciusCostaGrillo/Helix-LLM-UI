FROM node:20-alpine

WORKDIR /app

# Install dependencies based on lockfile
COPY package.json package-lock.json* ./
RUN npm ci

# Copy application files (overwritten by volume mount in dev)
COPY . .

# Expose Next.js port
EXPOSE 3000

# Start in development mode by default
CMD ["npm", "run", "dev"]
